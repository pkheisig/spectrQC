% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autospectral_interface.R
\name{create_autospectral_control_file}
\alias{create_autospectral_control_file}
\title{Create AutoSpectral Control File}
\usage{
create_autospectral_control_file(
  input_folder = "scc",
  af_folder = "af",
  include_af_folder = TRUE,
  cytometer = "Aurora",
  default_control_type = "cells",
  unknown_fluor_policy = c("empty", "by_channel", "filename"),
  output_file = "fcs_control_file.csv",
  custom_fluorophores = NULL
)
}
\arguments{
\item{input_folder}{Directory containing single-stained control FCS files.}

\item{af_folder}{Optional AF folder.}

\item{include_af_folder}{Logical. Include AF folder files in the control file.}

\item{cytometer}{Cytometer name used for channel-to-fluorophore mapping
(for example `"Aurora"`).}

\item{default_control_type}{Deprecated. Kept for backward compatibility and ignored.}

\item{unknown_fluor_policy}{How to fill unresolved fluorophores:
`"empty"` (recommended), `"by_channel"` (best-effort guess), `"filename"`.}

\item{output_file}{Path where the CSV will be saved (default: "fcs_control_file.csv").}

\item{custom_fluorophores}{Optional named vector to map filenames to fluorophore names.}
}
\value{
A data frame containing the control file information.

The generated file intentionally leaves `universal.negative` empty by default.
It sets `control.type = "cells"` only for AF rows and leaves non-AF rows empty
for manual completion.
}
\description{
Generates the CSV file required by the AutoSpectral package.
}
\examples{
args(create_autospectral_control_file)
\dontrun{
control_df <- create_autospectral_control_file(
  input_folder = "scc",
  cytometer = "Aurora",
  output_file = "fcs_control_file.csv"
)
head(control_df)
}
}
