% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unmixing_matrix.R
\name{save_unmixing_matrix}
\alias{save_unmixing_matrix}
\title{Save Unmixing Matrix to CSV}
\usage{
save_unmixing_matrix(W, file = "unmixing_matrix.csv")
}
\arguments{
\item{W}{Unmixing matrix}

\item{file}{Path to save}
}
\value{
Invisibly returns `NULL`; writes CSV to disk.
}
\description{
Save Unmixing Matrix to CSV
}
\examples{
tmp <- tempfile(fileext = ".csv")
M <- matrix(c(1, 0.2, 0.1, 1), nrow = 2, byrow = TRUE)
rownames(M) <- c("FITC", "PE")
colnames(M) <- c("B2-A", "YG1-A")
save_unmixing_matrix(M, tmp)
}
